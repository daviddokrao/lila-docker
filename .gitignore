.env
# Bản sao lưu .env vẫn chứa nguyên secret mà KHÔNG khớp luật `.env` ở trên —
# `deploy/.env.bak` đã nằm chờ ở đây từ 01/08, chỉ cần một lệnh `git add -A` là lọt.
*.env.bak
*.env.backup
.pnpm-store

conf/firebase-service-account.json
firebase.env

command/.cargo
command/repos
command/target
command/.intentionally-empty-file.o
database
repos

settings.env
settings.toml

.metals

# picfit-generated files
docker/assets/*.webp

.bsp
.scala-build
settings.json

# IntelliJ auto-generated files
.idea
